{
  "name": "advogo-seguro-cloudflare-staging",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dry-run": "wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "@cloudflare/containers": "^0.3.7",
    "wrangler": "^4.125.0"
  }
}
